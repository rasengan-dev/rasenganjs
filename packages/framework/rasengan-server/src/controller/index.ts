/**
 * @module Controller — route-grouping abstraction
 *
 * Controllers encapsulate a set of related routes under a shared
 * middleware chain and optional dependency injection.
 */

export { Controller } from './base.js';
export type { RouteHandler } from './base.js';
