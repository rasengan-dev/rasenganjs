## Unreleased

## 1.0.1 (2026-02-06)

fix: solve ERR_UNSUPPORTED_DIR_IMPORT #120

## 1.0.0 (2026-01-03)

Introduction of Kurama, a state manager library used in any React application.

It provides features like:

- Store creation via `createStore` utility function by defining `state` and `actions` in a Zustand way
- Support for middleware like `logger`, `persist` and `compose`

## 1.0.0-beta.0 (2026-01-03)
